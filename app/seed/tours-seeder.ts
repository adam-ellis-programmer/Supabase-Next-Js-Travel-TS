// import { createClient } from '@/lib/supabase/server'

// export class SeedDB {
//   async tours() {
//     const supabase = await createClient()
//     const response = await supabase.from('countries').delete().eq('id', null)

//     //...
//     const { error } = await supabase
//       .from('countries')
//       .insert({ id: 1, name: 'Mordor' })
//   }
// }
